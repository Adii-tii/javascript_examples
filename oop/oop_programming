for an object to exist it must have a class.
similarly for a class to be be meaningful it must have objects.
a class is like a blueprint that defines the objects that belong to that class.
objects belonging to the same class can have varying properties.
